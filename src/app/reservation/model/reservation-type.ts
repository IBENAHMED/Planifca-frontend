export interface createReservation {
  name: string,
  email: string,
  frontPath: string,
}
export interface reservationIonInformation {
  name: string,
  last_action: string,
  creation_date: string,
  statut: string,
}
